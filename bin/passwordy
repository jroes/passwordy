#!/usr/bin/env ruby

$:.unshift File.join(File.dirname(__FILE__), *%w[.. lib])

help = <<HELP
Passwordy generates passwords without a password database.

Basic Command Line Usage:
  passwordy <domain>    # Generate a password for a domain. Ex. www.google.com

HELP

require 'passwordy'

if ARGV.length != 1
  puts 'Please specify a domain to generate a password for'
  exit(1)
end

puts Passwordy::Generator.generate_password(ARGV[1])
